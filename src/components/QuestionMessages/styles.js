import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    messagesContainer: {
        paddingHorizontal: 20
    },
    message: {
        marginBottom: 5,
        fontWeight: 'bold'
    },
    errorMessage: {
        color: 'red',
    },
    warningMessage: {
        color: 'orange',
    }
});
