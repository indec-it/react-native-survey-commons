import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    actionButtons: {
        marginTop: 10,
        paddingBottom: 30,
        flex: 0.17,
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'center'
    }
});
