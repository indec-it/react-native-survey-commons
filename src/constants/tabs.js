import {enums} from './';

const surveyList = [
    {idGroup: enums.surveyState.OPENED, tabName: 'Pendientes'},
    {idGroup: enums.surveyState.CLOSED, tabName: 'Cerradas'}
];

export default {
    surveyList
};
