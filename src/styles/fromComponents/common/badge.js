import {StyleSheet} from 'react-native';

const defaultStyle = StyleSheet.create({
    primaryContainer: {
        backgroundColor: '#333'
    },
    primaryText: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 'bold'
    },
    secondaryContainer: {
        backgroundColor: '#e6e6e6'
    },
    secondaryText: {
        color: '#555',
        fontSize: 16,
        fontWeight: 'bold'
    }
});

export default {
    defaultStyle
};
